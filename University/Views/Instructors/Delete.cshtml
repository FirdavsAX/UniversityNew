@using University.Models.InstructorViewModels;
@model InstructorDisplayViewModel;

@{
    ViewData["Title"] = "Delete";
}
<h2>Delete instructor</h2>
<h4> Are you sure delete this?</h4>

<div>
<dl class="row">
        <dt>
            @Html.DisplayNameFor(item => item.FullName)
        </dt>
        <dd>
            @Html.DisplayFor(item => item.FullName)
        </dd>
        <dt>
            @Html.DisplayNameFor(item => item.Email)
        </dt>
        <dd>
            @Html.DisplayFor(item => item.Email)
        </dd>
        <dt>
            @Html.DisplayNameFor(item => item.Department)
        </dt>
        <dd>
            @Html.DisplayFor(item => item.Department)
        </dd>
    </dl>
    
<div>
    <form asp-action="Delete" class="">
        <input type="hidden" asp-for="Id" />
        <input type="submit" class="btn btn-danger" value="Delete"/>
        <a asp-action="Index">Back to list</a>
    </form>
</div>
    